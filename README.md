# Matrix

**Multi-Agent Task Runtime & Inter-session eXchange** — a local, project-scoped task management MCP server for multi-agent, multi-session work. Think of it as a lightweight local project manager, backed by SQLite, that AI agents can connect to via the Model Context Protocol.
